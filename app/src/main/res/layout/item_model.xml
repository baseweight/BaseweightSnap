<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Default indicator (star icon) -->
        <ImageView
            android:id="@+id/ivDefaultIndicator"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@string/default_model_indicator"
            android:src="@android:drawable/star_on"
            android:tint="#FFD700"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:visibility="visible" />

        <!-- Model name -->
        <TextView
            android:id="@+id/tvModelName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:textColor="?android:attr/textColorPrimary"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions"
            app:layout_constraintStart_toEndOf="@id/ivDefaultIndicator"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="SmolVLM2-256M-Video-Instruct" />

        <!-- Repository -->
        <TextView
            android:id="@+id/tvRepository"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="8dp"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="14sp"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions"
            app:layout_constraintStart_toStartOf="@id/tvModelName"
            app:layout_constraintTop_toBottomOf="@id/tvModelName"
            tools:text="ggml-org/SmolVLM2-256M-Video-Instruct-GGUF" />

        <!-- Size information -->
        <TextView
            android:id="@+id/tvSize"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="8dp"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="12sp"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions"
            app:layout_constraintStart_toStartOf="@id/tvModelName"
            app:layout_constraintTop_toBottomOf="@id/tvRepository"
            tools:text="Size: 279 MB" />

        <!-- Download date -->
        <TextView
            android:id="@+id/tvDownloadDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="8dp"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="12sp"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions"
            app:layout_constraintStart_toStartOf="@id/tvModelName"
            app:layout_constraintTop_toBottomOf="@id/tvSize"
            tools:text="Downloaded: Jan 15, 2025" />

        <!-- More options menu button -->
        <ImageButton
            android:id="@+id/btnMoreOptions"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/more_options"
            android:src="@android:drawable/ic_menu_more"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
